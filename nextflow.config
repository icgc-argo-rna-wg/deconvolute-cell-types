manifest {
    homePage = 'https://github.com/icgc-argo-rna-wg/deconvolute-cell-types'
    description = 'Deconvolute Cell Types'
    nextflowVersion = '>=20.10'
}

docker {
    enabled = true
    runOptions = '-u \$(id -u):\$(id -g)'
}
